springfox:
  documentation:
    swagger:
      v2:
        path: /api-docs
spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/study_blog
    username: root
    password: root
    driver-class-name: org.postgresql.Driver
    jpa:
      show-sql: true
      hibernate:
        ddl-auto: create-drop
  output:
    ansi:
      enabled: ALWAYS
